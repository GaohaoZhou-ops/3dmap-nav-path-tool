你需要在当前目录下实现一个web页面，功能如下：

1. 提供加载地图文件的按钮，点击后在3D交互区域对地图文件进行显示；
2. 提供一一个垂直高度的bar用来截取3D地图指定高度范围的点云并将其压缩成一个2D地图显示；
3. 3D交互区域与2D压缩地图需要同时显示；
4. 可以在2D地图上添加导航点，导航点在记录的时候要包含原始3D信息，xzy、rpy；
5. 选中2D地图上任意两点可以创建一条路径，但路径是有方向的，因为我想构建一张有向图；
6. 在创建路径或双击路径的时候可以配置路径的最大速度、最小速度、最大加速度、最小加速度；
7. 提供一个路径实施检测的功能，即如果路径中任意两点间无法相互到达联通，则用黄色显示，否则用绿色显示；
8. 提供一个导出json格式的按钮；
9. 提供查看服务状态、启动服务、终止服务的脚本，服务默认用本地21990端口；
10. 提供一个加载路径的按钮，加载完成后显示对应的配置，包括2D地图的截面；

我在当前目录的maps文件夹下放了一个示例地图文件；